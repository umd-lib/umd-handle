# frozen_string_literal: true

require 'test_helper'

# Simple sanity test to verify that tests are working
class SanityTest < ActiveSupport::TestCase
  test 'tests can be run' do
    assert true
  end
end
